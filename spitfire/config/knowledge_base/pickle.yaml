knowledge_base:
    name: grpc-server
    # accessible within the cluster
    host: "10.105.43.27" 
    port: 61111
    # dummy value (needed to match) 
    container_port: 61111 
    # below are accessible outside the cluster; 
    # these values can be autogenerated and just 
    # looked up in code later, for now specify here
    external_port: 30005 # Must be btwn 30000-32767 
